#Queries Quality and Percentage
select query_name,Round(avg(rating/position),2) as quality, Round(sum(if(rating <3,1,0))*100/count(rating),2) as 'poor_query_percentage' from Queries where query_name is NOT NULL group by query_name 
