#Product Sales Analysis III
# Write your MySQL query statement below
select s.product_id, s.year as first_year ,s.quantity,s.price from sales s left join product p on s.product_id=p.product_id 
where  (s.product_id,s.year) IN  (select product_id,min(year) from sales group by product_id) group by product_id, year, quantity
